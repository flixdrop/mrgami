import { SellerFormPipe } from './seller-form.pipe';

describe('SellerFormPipe', () => {
  it('create an instance', () => {
    const pipe = new SellerFormPipe();
    expect(pipe).toBeTruthy();
  });
});
